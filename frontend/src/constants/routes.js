/**
 * Centralized route path constants.
 * Used in both router config and navigation links — single source of truth (DRY).
 */
export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  BOOKMARKS: '/bookmarks',
};
